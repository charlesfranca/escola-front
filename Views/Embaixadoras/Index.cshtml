@{
    ViewData["Title"] = "Embaixadoras";
}

@await Component.InvokeAsync("Header")

<section id="menu">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                @Html.Partial("_NavLogged")
            </div>
        </div>
    </div>
</section>

<section>
    <div class="container">
        <h1 class="section-title">
            Embaixadoras
        </h1>

        <p>
            Preencha todas as informações para ter um perfil completo. Somente os campos com * são obrigatórios para emitir o certificado, mas preenchendo todos podemos personalizar ainda mais sua jornada.
        </p>
        <p>

        </p>
    </div>

    <div class="container embaixadora-box-cep">
        <h1 class="section-title">
            São Paulo - SP 
        </h1>
        <button class="escola-btn right-button">Alterar Cep</button>
    </div>
</section>

@await Component.InvokeAsync("Embaixadoras")

@await Component.InvokeAsync("EmbaixadorasSearch")