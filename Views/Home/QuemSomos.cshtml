@{ 
    ViewData["Title"] = "Home Page"; 
    string partner = (string)this.ViewContext.RouteData.Values["partner"];
    var partial = "_BannerEscoaDeVoce";
    if(!string.IsNullOrEmpty(partner)){
        partial = "_BannerBrilhante";
    }
} 

@section modalContent {
    @Html.Partial("_SignupForm")
    @Html.Partial("_LoginForm")
}

@if(!User.Identity.IsAuthenticated){
    
    @Html.Partial(partial);

    <section id="menu">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    @Html.Partial("_NavLogged")
                </div>
            </div>
        </div>
    </section>
}else{
    @await Component.InvokeAsync("Header");
}

<section id="papo-de-escolete">

    <div class="container">
    <h2 class="sub-title">Quem Somos</h2>
    <br>
        <div class="papo quem-somos">
            <p>
 A Escola de Você, plataforma de cursos online para mulheres, desenvolveu uma metodologia própria que transforma conceitos complexos em vídeos divertidos, recheados de dicas fáceis de aplicar no dia a dia. A Aliança Empreendedora, ONG especializada na formação de empreendedores, é parceira no desenvolvimento do conteúdo e responsável pela fase final do projeto que seleciona alunas para receber investimento e formações presencial.            </p>
        </div>
    </div>
</section>
