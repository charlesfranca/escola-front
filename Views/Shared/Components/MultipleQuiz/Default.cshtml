@model List<EscolaDeVoce.Services.ViewModel.PersonalityQuestionViewModel>

<section id="all-quizes">
    <div class="container">
        <div class="row">
            @foreach(var item in Model){
                <div class="col-md-5 col-md-offset-3">
                    @Html.Partial("_QuizQuestion", item)
                </div>
            }
        </div>
    </div>
</section>